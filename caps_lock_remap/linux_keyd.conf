[ids]

*

[main]

# Maps capslock to escape when pressed and control when held.
# capslock key toggles capslock state when pressed long
# capslock = overload(control, esc)
capslock = timeout(overload(control, esc), 1000, capslock))

# Remaps the escape key to capslock
# esc = capslock
